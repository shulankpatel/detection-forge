# Core dependencies — reliably installable; CI requires these to succeed.
# Rule loading/validation/tests need pyyaml + pytest; ruff lints; sigma-cli
# provides `sigma check` for Sigma rule linting. Flask serves the web UI and API.
flask>=2.0
gunicorn>=20.0  # WSGI server for production deployment (Render, Heroku, etc.)
pyyaml>=6.0
pytest>=8.0
ruff>=0.5
sigma-cli>=1.0
python-evtx>=0.7  # parse real EVTX attack telemetry (tests/test_real_telemetry.py)
