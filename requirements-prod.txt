# Production requirements for deployment (includes all backends)
# This file combines requirements.txt + requirements-backends.txt for deployment

# Core dependencies
flask>=2.0
flask-cors>=4.0
gunicorn>=20.0
pyyaml>=6.0
python-evtx>=0.7

# pySigma conversion backends (for Splunk, Sentinel/KQL, Elasticsearch)
pysigma>=0.11,<0.12
pysigma-backend-splunk>=1.1,<2
pysigma-backend-elasticsearch>=1.1,<2
pysigma-backend-kusto>=0.4,<0.5
