# Best-effort conversion backends (pySigma + per-SIEM plugins).
# These are OPTIONAL: CI installs them tolerantly (`|| echo ...`) so a backend
# packaging hiccup never turns the build red. Conversion is skipped for any
# backend whose plugin is unavailable.
pysigma>=0.11,<0.12
pysigma-backend-splunk>=1.1,<2
pysigma-backend-elasticsearch>=1.1,<2
pysigma-backend-kusto>=0.4,<0.5
